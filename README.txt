// This command runs the rviz environment, creates all the spheros with which we interface, 
creates our Cell nodes, which interface with the spheros, and launches our UI with which we change the formation

roslaunch NewSimulator simulation.launch



//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
To get the testing running
1)Download gtest
2)Unpack gtest
3)Run through the readme in gtest and setup gtest
4)To run the tests roscd into robot_driver and run 'make test'
